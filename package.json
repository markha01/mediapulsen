{
  "name": "mediapulsen",
  "version": "1.0.0",
  "description": "Content ROI Dashboard",
  "scripts": {
    "prestart": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
